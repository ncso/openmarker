
**************** clean and install

(
  set -eu
  export PATH=${M2_HOME}/bin:$PATH
  export MAVEN_OPTS="-Xmx512m -XX:MaxPermSize=128m"
  mvn --fail-fast -Dmdep.outputFile=mdep.classpath \
    dependency:build-classpath clean install
)

****************
